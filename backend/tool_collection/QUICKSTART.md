# 快速开始指南 - Medical AI Agent Tools

这是一个5分钟快速入门指南，帮助你快速上手使用病理知识问答智能体工具集。

---

## 🎯 目标

通过本指南，你将：
1. ✅ 了解工具集的基本结构
2. ✅ 学会如何扫描和注册工具
3. ✅ 学会如何为智能体分配工具
4. ✅ 学会如何测试工具功能
5. ✅ 学会如何在对话中使用工具

---

## 📦 第一步：查看工具列表

所有工具都位于 `backend/tool_collection/mcp/` 目录下：

```
tool_collection/mcp/
├── patient_management_tools.py    # 患者管理工具 (6个)
├── case_library_tools.py          # 病例检索工具 (6个)
├── knowledge_base_tools.py        # 知识库工具 (5个)
├── care_plan_tools.py             # 康复计划工具 (4个)
├── diagnostic_assistance_tools.py # 诊断辅助工具 (3个)
└── README.md                      # 详细文档
```

**快速查看所有工具：**
```bash
# 查看工具目录汇总文档
cat backend/tool_collection/TOOL_CATALOG.md
```

---

## 🔧 第二步：扫描并注册工具到数据库

### 方式1：通过API扫描（推荐）

```bash
# 启动后端服务
cd backend
python main_service.py

# 调用扫描接口
curl -X GET "http://localhost:5010/api/tool/scan_tool" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 方式2：通过Python脚本

```python
import asyncio
from services.tool_configuration_service import update_tool_list

async def scan_tools():
    await update_tool_list(
        tenant_id="your_tenant_id",
        user_id="admin"
    )
    print("✅ 工具扫描完成！")

# 运行扫描
asyncio.run(scan_tools())
```

**扫描结果：**
- 系统会自动扫描所有MCP工具
- 工具信息会保存到 `nexent.ag_tool_info_t` 表
- 可以在管理员端查看工具列表

---

## 🎨 第三步：为智能体分配工具

### 方式1：通过管理员端界面（推荐）

1. 登录管理员端：`http://localhost:3000/admin`
2. 导航到 **智能体配置** → **工具分配**
3. 选择你的医生端主智能体
4. 从工具库中拖拽工具到右侧（已分配）
5. 点击 **保存**

**建议配置（核心工具）：**
- ✅ `get_patient_basic_info` - 获取患者基本信息
- ✅ `get_patient_timeline` - 获取诊断时间线
- ✅ `search_medical_cases` - 搜索相似病例
- ✅ `get_case_detail` - 获取病例详情
- ✅ `search_knowledge` - 搜索知识库
- ✅ `differential_diagnosis` - 鉴别诊断

### 方式2：通过API分配

```python
import asyncio
from services.tool_configuration_service import update_tool_info_impl

async def assign_tools():
    # 分配工具到智能体
    await update_tool_info_impl(
        request={
            "agent_id": 1,  # 你的医生端主智能体ID
            "tool_id": 123,  # 工具ID（从数据库获取）
            "enabled": True,
            "params": {}  # 工具参数（可选）
        },
        tenant_id="your_tenant_id",
        user_id="admin"
    )
    print("✅ 工具分配成功！")

asyncio.run(assign_tools())
```

---

## 🧪 第四步：测试工具功能

### 测试单个工具

```python
# 示例：测试患者基本信息工具
from tool_collection.mcp.patient_management_tools import get_patient_basic_info

async def test_patient_info():
    result = await get_patient_basic_info(
        patient_id=1,
        tenant_id="test_tenant",
        include_summary=True
    )
    print("患者信息:", result)

import asyncio
asyncio.run(test_patient_info())
```

### 通过API测试

```bash
# 测试工具验证接口
curl -X POST "http://localhost:5010/api/tool/validate" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "tool_id": 123,
    "test_params": {
      "patient_id": 1,
      "tenant_id": "test_tenant"
    }
  }'
```

---

## 💬 第五步：在对话中使用工具

### 示例对话1：查询患者信息

**用户（医生）：** "给我看看患者张三的情况"

**智能体执行流程：**
1. 调用 `get_patient_basic_info` 获取基本信息
2. 调用 `get_patient_timeline` 获取时间线
3. 调用 `get_patient_examination_reports` 获取最新报告
4. 综合信息生成回答

**智能体回答示例：**
```
患者张三，男性，65岁，病案号MR20240001。

基本情况：
• 主要诊断：肺腺癌T2N1M0（II期）
• 入院日期：2024-10-15
• 联系电话：138****1234

诊断时间线：
• 2024-10-15: 入院 - 主诉咳嗽、胸痛2月余
• 2024-10-16: 检查 - 胸部CT示左肺上叶占位性病变
• 2024-10-18: 诊断 - 支气管镜活检确诊肺腺癌
• 2024-10-25: 治疗 - 行左肺上叶切除术+淋巴结清扫术

最新检查报告：
• 2024-11-15 病理报告：肺腺癌，中分化，无脉管侵犯
• 2024-11-10 血液检查：白细胞8.5×10⁹/L，血红蛋白130g/L
```

---

### 示例对话2：病例检索

**用户（医生）：** "查找肺腺癌伴淋巴结转移的相似病例"

**智能体执行：**
```python
# 自动调用
result = await search_medical_cases(
    query="肺腺癌 淋巴结转移",
    tenant_id="tenant_123",
    limit=5
)
```

**智能体回答示例：**
```
为您找到5个相似病例：

1. 病例 #2024098 - 肺腺癌T2N2M0
   • 相似度：95%
   • 关键症状：咳嗽、胸痛、消瘦
   • 治疗方案：手术+术后辅助化疗
   • 预后：良好，已随访2年无复发

2. 病例 #2024067 - 肺腺癌T1N1M0
   • 相似度：88%
   • 关键症状：咳嗽、咯血
   • 治疗方案：楔形切除+淋巴结清扫
   • 预后：良好

...（显示更多病例）
```

---

### 示例对话3：诊断辅助

**用户（医生）：** "患者60岁男性，咳嗽、胸痛、咯血，可能是什么病？需要做什么检查？"

**智能体执行：**
```python
# 自动调用鉴别诊断工具
diagnosis = await differential_diagnosis(
    symptoms=["咳嗽", "胸痛", "咯血"],
    patient_demographics={"age": 60, "gender": "male"},
    tenant_id="tenant_123"
)

# 自动调用检查建议工具
tests = await suggest_next_tests(
    patient_id=1,
    current_diagnosis="肺癌（疑似）",
    tenant_id="tenant_123"
)
```

**智能体回答示例：**
```
鉴别诊断分析：

根据症状（咳嗽、胸痛、咯血）和患者信息（60岁男性），
可能的诊断包括：

1. 肺癌（概率：75%）
   • 支持证据：年龄、症状组合、高风险人群
   • 匹配症状：3/3
   • 建议检查：胸部CT、支气管镜、肿瘤标志物

2. 肺结核（概率：15%）
   • 支持证据：咳嗽、咯血症状
   • 匹配症状：2/3
   • 建议检查：痰涂片、结核菌素试验

3. 支气管扩张（概率：8%）
   • 支持证据：咯血症状
   • 匹配症状：1/3
   • 建议检查：胸部CT

推荐的下一步检查：
✅ 必需检查（高优先级）：
  1. 胸部CT增强扫描 - 明确肺部病变性质
  2. 支气管镜检查+活检 - 获取病理诊断
  3. 肿瘤标志物检测 - 辅助诊断

🟢 推荐检查（中优先级）：
  4. PET-CT - 评估全身转移情况
  5. 肺功能检查 - 评估手术耐受性
```

---

## 📊 常见使用场景

### 场景1：快速了解患者
```
医生: "患者ID 123的基本情况"
工具: get_patient_basic_info(patient_id=123)
```

### 场景2：查看诊断历程
```
医生: "这个患者的诊断时间线"
工具: get_patient_timeline(patient_id=123)
```

### 场景3：学习病例
```
医生: "肺癌有哪些经典病例"
工具: get_classic_cases_by_disease(disease_type="肺癌")
```

### 场景4：知识查询
```
医生: "肺癌的TNM分期标准"
工具: search_diagnosis_guidelines(disease_name="肺癌", guideline_type="staging")
```

### 场景5：制定康复计划
```
医生: "为患者创建术后康复方案"
工具: generate_care_plan_suggestions() + create_care_plan()
```

---

## 🐛 常见问题排查

### 问题1：工具不显示在管理员端

**原因：** 工具未扫描或名称不符合规范

**解决方案：**
```bash
# 1. 检查工具名称是否符合规范（只能包含字母、数字、下划线）
# 2. 重新扫描工具
curl -X GET "http://localhost:5010/api/tool/scan_tool" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. 检查数据库
# SELECT * FROM nexent.ag_tool_info_t WHERE is_available = true;
```

---

### 问题2：工具调用失败

**原因：** 参数错误或服务未启动

**解决方案：**
```python
# 1. 检查参数是否正确
# 2. 查看日志
tail -f logs/app.log

# 3. 测试单个工具
from tool_collection.mcp.patient_management_tools import get_patient_basic_info
result = await get_patient_basic_info(patient_id=1, tenant_id="test")
print(result)
```

---

### 问题3：智能体不调用工具

**原因：** 工具未分配或描述不清晰

**解决方案：**
1. 检查工具是否已分配给智能体
2. 检查工具是否已启用（enabled=True）
3. 优化工具描述，使其更清晰
4. 在智能体prompt中添加工具使用指导

---

## 📚 下一步学习

- 📖 [完整工具目录](./TOOL_CATALOG.md) - 查看所有25个工具的详细说明
- 📖 [工具使用指南](./mcp/README.md) - 深入了解工具配置和使用
- 📖 [开发新工具](../../doc/tool-development-guide.md) - 学习如何开发自定义工具
- 📖 [Agent配置](../../doc/agent-configuration.md) - 了解智能体配置最佳实践

---

## 💡 最佳实践建议

### 1. 工具选择
- 从核心工具开始（6-8个）
- 逐步添加推荐工具
- 根据使用情况调整

### 2. 工具配置
- 为每个门户配置专门的工具集
- 医生端：重点配置患者管理和诊断辅助工具
- 学生端：重点配置病例学习和知识库工具

### 3. 性能优化
- 避免在一次对话中调用过多工具
- 使用缓存减少重复查询
- 优化工具描述，提高调用准确性

### 4. 监控和优化
- 定期查看工具调用日志
- 分析工具使用频率
- 根据反馈优化工具功能

---

## 🎉 恭喜！

你已经完成了快速入门！现在你可以：
- ✅ 扫描和注册工具
- ✅ 为智能体分配工具
- ✅ 测试工具功能
- ✅ 在对话中使用工具

开始使用工具提升你的病理知识问答智能体吧！🚀

---

**需要帮助？**
- 查看 [完整文档](./mcp/README.md)
- 提交 [GitHub Issue](https://github.com/ModelEngine-Group/nexent/issues)
- 加入 [Discord社区](https://discord.gg/tb5H3S3wyv)
